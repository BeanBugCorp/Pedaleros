// Active event id, sourced from the environment (set VITE_SUPABASE_EVENT_ID
// in your .env). Shared by the guest, auction, and hub pages.
export const EVENT_ID = import.meta.env.VITE_SUPABASE_EVENT_ID
