// Provider hook for the auction view. Skeleton only — add Supabase queries here
// when the auction needs data (e.g. pairs and sales totals for an event).
export function useAuctionData() {
  return {}
}
